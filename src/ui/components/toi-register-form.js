import React from 'react';
import { RaisedButton } from 'material-ui';

const ToiRegisterForm = props => (
  <div>
    <p>Verify the displayed information and submit to complete registration.</p>
    <RaisedButton primary={true} label="Submit" />
  </div>
);

export default ToiRegisterForm;